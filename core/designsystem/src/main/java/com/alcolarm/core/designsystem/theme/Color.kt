package com.alcolarm.core.designsystem.theme

import androidx.compose.ui.graphics.Color

/** Clear Signal — dark base, high contrast, amber accent. */
object ClearSignalColors {
    val NearBlack = Color(0xFF0B0D10)
    val Surface = Color(0xFF151922)
    val SurfaceElevated = Color(0xFF1E2430)
    val Outline = Color(0xFF3A4254)

    val OnDark = Color(0xFFF4F6FA)
    val OnDarkMuted = Color(0xFFB8C0D0)

    val Amber = Color(0xFFFFB020)
    val AmberDim = Color(0xFFE09810)
    val AmberContainer = Color(0xFF3D2E0A)

    val TealSupport = Color(0xFF2EC4B6)
    val Danger = Color(0xFFFF5A5F)
    val Success = Color(0xFF3DDC97)
}
